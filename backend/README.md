# Configure the Backend 

```bash
npm install express mongoose axios dotenv cors bcryptjs jsonwebtoken axios cookie-parser express-async-handler
npm i -D nodemon
```

# Run Backend 
```bash
cd backend
npm run server
```